/* Base styles */
body {
  background-color: #191923; /* New background color */
  color: #fbfef9; /* New light text color */
  font-family: "system-ui", Courier, monospace; /* Retro pixel-like font */
  margin: 0;
  padding: 0;
}

/* App container */
.App {
  max-width: 600px;
  margin: 2vh auto;
  padding: 20px;
  border: 5px solid #0e79b2; /* New accent color */
  border-radius: 10px;
  background: #0b0909; /* Dark gray background */
}

/* Chat container */
.chat-container {
  display: flex;
  flex-direction: column;
  height: 85vh;
  background: #1e1e1e; /* Slightly lighter dark background for contrast */
  border: 2px solid #0e79b2; /* New accent color */
  border-radius: 10px;
  overflow: hidden;
}

/* Messages */
.messages {
  display: flex;
  flex-direction: column; /* Stack messages vertically */
  flex: 1;
  overflow-y: auto;
  padding: 10px;
}

/* Message bubbles */
.message {
  display: inline-block; /* Adjust width relative to content */
  max-width: 80%; /* Limit the width of the message bubble to 80% of the container */
  padding: 10px;
  border-radius: 10px;
  background: #0d0b0b; /* Darker gray for message bubbles */
  border: 2px solid #0e79b2; /* Deep blue accent color */
  color: #fbfef9; /* Light text color */
  font-size: 14px;
  margin-bottom: 10px;
  word-wrap: break-word; /* Ensure long words break to fit within the bubble */
}

/* User-specific styles */
.message.user {
  align-self: flex-end; /* Align user messages to the right */
  background: #0d0b0b; /* Darker gray for user messages */
}

/* Bot-specific styles */
.message.bot {
  align-self: flex-start; /* Align bot messages to the left */
  background: #0d0b0b; /* Darker gray for bot messages */
}


/* Chat form */
.chat-form {
  display: flex;
  border-top: 2px solid #0e79b2; /* New accent color */
}

.chat-form input {
  flex: 1;
  padding: 10px;
  border: none;
  border-radius: 0;
  background: #2c2c2c; /* Dark gray */
  color: #fbfef9; /* New light text color */
  font-size: 14px;
}

.chat-form button {
  padding: 10px;
  background: #0e79b2; /* New accent color */
  color: #fbfef9; /* New light text color */
  border: none;
  cursor: pointer;
  font-size: 14px;
  transition: background 0.3s;
}

.chat-form button:hover {
  background: #0c5a8a; /* Slightly darker blue for hover effect */
}
